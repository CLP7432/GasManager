import React from 'react';
import Layout from '../../components/common/Layout';
import CombustibleForm from '../../components/inventarios/CombustibleForm';

const CombustibleFormPage = () => {
    return (
        <Layout>
            <CombustibleForm />
        </Layout>
    );
};

export default CombustibleFormPage;