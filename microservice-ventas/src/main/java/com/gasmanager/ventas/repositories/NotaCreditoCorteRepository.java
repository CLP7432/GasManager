package com.gasmanager.ventas.repositories;

import com.gasmanager.ventas.entities.core.NotaCreditoCorte;
import org.springframework.data.jpa.repository.JpaRepository;

public interface NotaCreditoCorteRepository extends JpaRepository<NotaCreditoCorte, Long> {
}